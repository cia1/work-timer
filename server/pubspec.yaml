name: timer_server
description: A sample command-line application.
version: 0.2.3
repository: https://ciamon@bitbucket.org/ciamon/timer.git
environment:
  sdk: ^3.8.1
publish_to: none

# Add regular dependencies here.
dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  timer_lib:
    path: '../lib'

dev_dependencies:
  lints: ^6.0.0
  meta: ^1.18.1
  test: ^1.24.0