name: timer_lib
description: A starting point for Dart libraries or applications.
version: 1.0.3
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  map_validation: ^1.2.0
  meta: ^1.18.1
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
